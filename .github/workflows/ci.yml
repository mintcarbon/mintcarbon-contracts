name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
          components: rustfmt, clippy

      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Build contracts
        run: |
          cargo build --target wasm32-unknown-unknown --release -p carbon-token
          cargo build --target wasm32-unknown-unknown --release -p marketplace
          cargo build --target wasm32-unknown-unknown --release -p escrow
          cargo build --target wasm32-unknown-unknown --release -p verification-records
          cargo build --target wasm32-unknown-unknown --release -p audit-log
          cargo build --target wasm32-unknown-unknown --release -p governance

      - name: Run tests
        run: cargo test
